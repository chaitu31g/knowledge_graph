# ============================================================
# CircuitAI Backend — Google Colab Requirements
# ============================================================
# Install all dependencies in a single Colab cell with:
#   !pip install -r requirements.txt
#
# NOTE: Versions are intentionally loosely pinned so pip can
# resolve conflicts (e.g. chromadb pins its own fastapi version).

# --- Core API (let chromadb pick the compatible fastapi version) ---
fastapi
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0

# --- Vision Model (Qwen2.5-VL) ---
transformers>=4.45.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
qwen-vl-utils>=0.0.8

# --- PDF → Image Pipeline ---
pdf2image>=1.16.0
Pillow>=10.0.0

# --- RAG Stack ---
chromadb>=1.0.0
sentence-transformers>=3.0.0
flashrank==0.2.10

# --- Utilities ---
numpy==2.2.4
